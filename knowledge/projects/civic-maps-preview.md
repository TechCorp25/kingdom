# CivicMaps-preview — brief (snapshot)

- **Slug:** `civic-maps-preview`
- **On disk:** `projects/civic-maps-preview/` (**snapshot**, not an active clone)
- **Status:** snapshot — tracked for reference, not under active development here.
- **Stack:** Node/Express + **PostgreSQL** (NOT the Kingdom Postgres — separate project
  business DB). See `policies/stack-boundaries.md`.

## Notes
- CivicMAPS is the first tracked project cited in the Kingdom design, but current active
  work is IlluminateMyGallery.
- ⚠️ There is a `design-system/` inside this snapshot — it is **CivicMAPS's**, unrelated
  to IlluminateMyGallery's canonical design system. Don't cross them.
- Do not "fix" or build on a snapshot as if it were the canonical repo; if it becomes
  active, replace this brief with a full one from `_template.md` and clone properly.
