# lensflow — brief (snapshot)

- **Slug:** `lensflow`
- **On disk:** `projects/lensflow/` (**snapshot**, not an active clone)
- **Status:** snapshot — tracked for reference, not under active development here.
- **Stack:** TBD — confirm from the snapshot before assuming; do not infer from another
  project's stack (`policies/stack-boundaries.md`).

## Notes
- Registered in the tracked set alongside `illuminate-my-gallery` and
  `civic-maps-preview`.
- Do not build on a snapshot as if canonical. If it becomes active, clone properly and
  expand this brief from `_template.md`.
